/** Telegram “About” max length (chars) — must match panel `BIO_MAX` / Account Manager UI. */
export const TELEGRAM_PROFILE_BIO_MAX_LENGTH = 70;

/** Hard clamp upper bound in JSON parser when caller does not pass a lower max. */
export const PROFILE_GENERATION_MAX_BIO_LENGTH = 500;
